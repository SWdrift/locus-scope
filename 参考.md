# Locus Scope：从 OCI 迁移到 npm 生态

目标：移除现有 OCI package/distribution 设计，将 Locus package 迁移到 **npm package model + npm Registry protocol**。Locus 继续负责 Scope 与图语义；包的身份、版本、依赖、分发和 Registry 语义尽量复用 npm 生态。

同时支持：

```text
Pure Locus
→ 无 Node/npm/pnpm
→ locus-scope + locus-pkg

npm ecosystem
→ npm/pnpm 管理依赖
→ @locus/scope + locus-scope-node
```

两种模式使用同一种 Locus package 和 Scope 语义，仅 package installation/resolution 来源不同。

## 1. Package 模型

将 **Locus package 定义为普通 npm-compatible package**。

`package.json` 是 package-level metadata 的唯一真相：

```json
{
  "name": "@example/network",
  "version": "1.0.0",
  "description": "Example network scope",
  "dependencies": {
    "@example/base": "^1.0.0"
  },
  "locus": {
    "entry": "./locus.yaml"
  }
}
```

职责划分：

```text
package.json
→ name / version / dependencies / semver
→ registry / distribution / repository / license 等包语义

locus.yaml + definition files
→ Entity / Relation / Group / Import
→ Scope / graph semantics
```

不要在 `locus.yaml` 中重复定义 package name/version/dependencies。

`dependencies` 表示 package dependency，不等同于 Scope import：

```text
package.json dependencies
→ 当前 package 所需的 package universe

Locus import
→ 当前 Scope 实际引用哪些 package / Scope
```

与代码一致：package 被安装，不意味着其中代码一定被 `import`。

删除 OCI 相关：

```text
OCI artifact
OCI manifest / media type
OCI tag / digest resolution
OCI publish / pull / install
OCI-specific package metadata
```

不保留 OCI compatibility layer，除非存在明确的外部兼容需求。

## 2. Pure Locus 模式

提供：

```text
locus-scope
locus-pkg
```

不要求安装 Node/npm/pnpm。

### locus-pkg

`locus-pkg` 是 **Pure Locus 环境中的 standalone npm-compatible package manager**。

首版 CLI：

```text
locus-pkg install [<package-spec>...]
locus-pkg uninstall <package>...
locus-pkg update [<package>...]
locus-pkg list
locus-pkg pack
locus-pkg publish
```

主要语义向 npm 看齐：

```text
locus-pkg install
→ 读取 package.json
→ 根据 locus.lock 恢复或解析 dependencies
→ 安装到 .locus/packages
→ 更新 locus.lock

locus-pkg install @example/network@^1
→ 写入 package.json dependencies
→ resolve / install
→ 更新 locus.lock

locus-pkg uninstall @example/network
→ 删除 dependency
→ 更新 package graph / locus.lock / installed packages

locus-pkg update
→ 在 package.json constraints 内重新解析

locus-pkg list
→ 显示 resolved dependency tree

locus-pkg pack
→ 生成 npm-compatible package tarball

locus-pkg publish
→ 发布 npm-compatible package 到 npm Registry
```

首版可提供少量通用参数：

```text
--registry <url>
--frozen-lockfile
--offline
--json
```

不要将内部机制扩展成大量独立 CLI，例如：

```text
resolve
fetch
cache
lock
registry
dependency
```

### 实现边界

`locus-pkg` 应保持为薄 package-manager facade。

Registry、npm semver、dependency resolution、tarball/integrity 等能力优先复用成熟 Go 实现，不自行重新设计 npm resolver。

优先评估：

```text
deps.dev/util/resolve/npm
deps.dev/util/semver
```

Locus 自身主要负责：

```text
npm Registry adapter
package download / integrity / extraction
.locus installation layout
locus.lock persistence
npm-compatible auth/config
Locus package validation
CLI
```

不要为了兼容 npm 而复制完整 npm installer。

Pure Locus 不需要实现：

```text
node_modules hoisting
npm/pnpm-specific node_modules layout
peer dependency installation layout
lifecycle scripts
native addon rebuild
npm scripts
workspace installer semantics
```

其中“不支持”指 **Locus 不承担这些 installer/runtime 语义**，而不是重新设计另一套对应机制。

### 本地布局

Pure Locus 项目使用：

```text
.locus/
  packages/
  cache/
  ...

locus.lock
```

`.locus/packages` 存放与 npm/pnpm 完全相同的 npm package 内容；Pure Locus 只定义自己的安装位置和 resolved package layout，不要求复制 npm/pnpm 的 `node_modules` 物理布局。

## 3. locus.lock

`locus.lock` 文件格式由 Locus 保持简单，但 **package resolution 语义向 npm lock 看齐**：

```text
package.json
→ dependency constraints

locus.lock
→ resolved concrete package graph
```

至少固定：

```text
package name
resolved version
registry/source
integrity
transitive dependency edges
```

目标：

```text
相同 package.json + locus.lock
→ 相同 resolved package universe
```

存在 lock 时默认按照 lock 安装；只有 update、显式变更 dependency 或删除 lock 后才重新解析可兼容版本。

`locus.lock` 不承担 Scope graph/import 语义。

> `locus.lock` is used only by Pure Locus mode. npm/pnpm modes use their native lockfiles and package environments; `@locus/scope` must not create, modify, or depend on `locus.lock`.

## 4. Registry authentication

`locus-pkg` 不建立自己的用户账户体系，也不维护独立 credential store。

Registry authentication 兼容 npm 配置模型，优先读取：

```text
CLI / environment override
project .npmrc
user ~/.npmrc
```

支持 registry-scoped token，例如：

```ini
registry=https://registry.example.com/

//registry.example.com/:_authToken=${NPM_TOKEN}
```

安装私有 package 与 `locus-pkg publish` 均复用该认证。

要求：

```text
token 不写入 package.json
token 不写入 locus.lock
token 不写入 .locus package state
```

首版以 Bearer token 为主要认证方式，不要求重新实现完整 `npm login`、网页登录或 2FA 交互流程。

如果用户已经通过 npm/pnpm 建立 `.npmrc` 凭据，`locus-pkg` 应能够直接复用。

Pure Locus 的含义是：

> 不依赖 Node/npm/pnpm executable，而不是与 npm 用户环境隔离。

## 5. npm ecosystem 模式

发布 npm package：

```text
@locus/scope
```

CLI：

```text
locus-scope-node
```

npm/pnpm 负责：

```text
package install
dependency resolution
package lock
cache
node_modules / virtual store
registry auth
```

Locus 不再维护：

```text
.locus/packages
locus.lock
```

普通 Scope package 可以直接：

```bash
npm install @example/network
```

或：

```bash
pnpm add @example/network
```

项目可将 Scope package 直接放入 `package.json`：

```json
{
  "dependencies": {
    "@example/network": "^1.0.0"
  }
}
```

然后由 `@locus/scope` 在当前 Node package environment 中解析 package，并读取其：

```json
{
  "locus": {
    "entry": "./locus.yaml"
  }
}
```

不要硬编码：

```text
node_modules/<package>
```

应依赖 Node/package resolution 机制，从而自然支持 npm 与 pnpm。**只需要发布标准 npm package。**

## 6. 两种模式的关系

两种模式区别仅在 package manager：

```text
Pure Locus:

npm Registry
    ↓
locus-pkg
    ↓
.locus/packages + locus.lock
    ↓
locus-scope


npm ecosystem:

npm Registry
    ↓
npm / pnpm
    ↓
package environment + package lock
    ↓
@locus/scope / locus-scope-node
```

最终加载的：

```text
package.json
Locus entry
Entity
Relation
Import
Scope graph
```

必须具有相同语义。

`locus-scope` 与 `@locus/scope` 不形成两套 Locus protocol。

CLI 名称区分：

```text
locus-scope
→ standalone / Pure Locus

locus-scope-node
→ npm ecosystem
```

因此：

```text
独立 locus-scope
npm -g @locus/scope
```

可以同时存在，不发生 executable name 冲突。

模式也不要根据 CLI 是如何安装的进行猜测：

```text
locus-scope      ≠ 自动意味着某种 package metadata
locus-scope-node ≠ 另一套 Scope protocol
```

二者只是不同 package-environment integration。

## 7. 本地 Registry 与 E2E

E2E 使用 **Verdaccio** 作为临时本地 npm Registry。

推荐默认：

```text
http://127.0.0.1:4873
```

Verdaccio 仅作为测试基础设施。

测试要求：

```text
不依赖公网 npm Registry
不修改用户全局 npm registry
不污染用户全局 npm/pnpm 配置
```

npm/pnpm 使用测试目录 `.npmrc` 或显式：

```text
--registry http://127.0.0.1:4873
```

准备至少两个 package：

```text
@example/base
@example/app
```

关系：

```text
@example/app
└── dependency → @example/base@^1
```

二者均包含有效 Locus Scope。

`app` 的 Locus graph 显式 import/use `base` 中的 Scope 内容，从而同时验证：

```text
package dependency
≠
Scope import
```

但二者可以协同工作。

## 8. E2E 验收标准

### A. Publish

启动临时 Verdaccio。

准备并发布：

```text
@example/base@1.0.0
@example/app@1.0.0
```

使用标准 npm publish 完成测试 fixture 发布。

验收：

```text
两个 package 均可从本地 Registry 获取
package metadata 正确
dependencies 正确
tarball 可下载
integrity 信息可获得
```

### B. Pure Locus install

创建完全独立的临时项目。

执行：

```bash
locus-pkg install @example/app@^1
```

验证完整流程：

```text
读取 registry metadata
resolve app
resolve transitive base
download tarballs
verify integrity
install into .locus/packages
write locus.lock
```

随后：

```bash
locus-scope ...
```

验证：

```text
app Scope 成功加载
base package 可解析
app 对 base 的显式 Scope import 成功
Entity / Relation 等预期内容可查询
locus.lock 保存实际 resolved version
locus.lock 保存 integrity
```

Pure Locus 被测路径不得调用：

```text
node
npm
pnpm
```

Node 仅允许作为 E2E infrastructure 启动 Verdaccio。

### C. Pure Locus publish

使用：

```bash
locus-pkg pack
locus-pkg publish
```

将新的 fixture package 发布到 Verdaccio。

验证：

```text
生成 npm-compatible package
publish 成功
Registry 可重新获取
npm/pnpm 能正常安装该 package
```

这一项证明：

> `locus-pkg` 发布的包是真正 npm-compatible，而不是仅能被 Locus 自己读取。

### D. npm mode

创建新的干净项目：

```bash
npm install @example/app --registry ...
npm install @locus/scope --registry ...
```

通过：

```bash
locus-scope-node ...
```

执行与 Pure Locus 相同的 Scope 验证。

结果必须等价。

### E. pnpm mode

创建另一干净项目：

```bash
pnpm add @example/app --registry ...
pnpm add @locus/scope --registry ...
```

执行：

```bash
locus-scope-node ...
```

进行相同验证。

npm 与 pnpm 模式均不得依赖特定 `node_modules` 物理布局。

### F. Lock / reproducibility

发布：

```text
@example/base@1.1.0
```

保持：

```json
"@example/base": "^1.0.0"
```

验证 Pure Locus：

```text
无 lock / 首次 resolution
→ 可以解析到新的兼容版本

已有 locus.lock
→ install 固定使用 lock 中版本

locus-pkg update
→ 可以重新解析到新的兼容版本

--frozen-lockfile
→ lock 与 package.json 不一致时失败
```

npm/pnpm 分别使用自己的 lockfile，并验证相同的 reproducibility 原则。

### G. Authentication

Verdaccio E2E 增加需要认证的 publish/private package 场景。

验证：

```text
locus-pkg 从测试 .npmrc 或 NPM_TOKEN 获取 token
authenticated publish 成功
authenticated install 成功
缺少/错误 token 时明确失败
locus.lock / .locus / package.json 中不存在凭据
```

## 9. 完成标准

迁移完成时必须满足：

1. 核心代码和文档不再依赖 OCI package/distribution。
2. Locus package 是标准 npm-compatible package。
3. `package.json` 是 package metadata 唯一真相。
4. package dependency 与 Locus Scope import 保持分层。
5. `locus-pkg` 提供 `install / uninstall / update / list / pack / publish`。
6. `locus-pkg` 无 Node/npm/pnpm 依赖即可完成 Pure Locus package lifecycle。
7. npm semver/dependency resolution 优先复用成熟 Go 实现，不自行造 resolver。
8. `.locus/packages + locus.lock` 只属于 Pure Locus。
9. npm ecosystem 模式完全使用 npm/pnpm 自己的 package environment 与 lock。
10. `@locus/scope` 可以作为普通 npm dependency 使用，并提供 `locus-scope-node`。
11. npm 与 pnpm 均通过 E2E。
12. Pure Locus、npm、pnpm 三种路径加载得到等价 Scope。
13. `locus-pkg publish` 产生的 package 可以被 npm/pnpm 正常安装。
14. Registry auth 兼容 `.npmrc` / environment token，不建立独立 credential store。
15. 安装 package 不执行 npm lifecycle scripts。
16. E2E 全程使用临时 Verdaccio，不依赖公网 Registry 或用户全局配置。
17. 删除 OCI 迁移后失去用途的实现、抽象和文档，不保留无意义兼容代码。

整体原则：

> **Locus 使用 npm 生态定义 package、版本、依赖和分发；Pure Locus 只提供一个无需 Node 的轻量 npm-compatible package-manager implementation。Locus 自己专注于 Scope、图和后续能力语义，而不是重新发明通用包管理生态。**

我的补充：

**Pure Locus**安装布局必须允许同一 package 的多个 resolved version 共存；package dependency 的解析以 importer package 为上下文，根据 `locus.lock` 中的 dependency edges 定位具体版本，不采用全局 package-name → single-version 映射。这点若使用npm生态因该自然满足？

`locus-scope` 需移除 OCI source/package 逻辑，并将外部 Scope 解析统一收敛为 package-aware loader：Pure Locus 从 `.locus/packages + locus.lock` 解析，npm 模式从 Node package environment 解析；两者共享同一 Scope/Graph 语义与基于 package identity 的稳定实体身份。
